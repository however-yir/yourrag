coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 5%
    patch:
      default:
        target: 50%
        threshold: 5%
  comment:
    layout: "reach,diff,flags,files"
    behavior: default
    require_changes: false